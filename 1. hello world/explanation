hello world.c 분석

첫줄은 헤더파일을 선언하는 코드이다

헤더파일(.h)은 코드를 실행하는데 있어 필요한 함수등을 미리 만들어두고 불러올때 사용한다
여기서 stdio.h 는  Standard Input/Output library의 준말로 
여러 입출력 함수등을 포함하고 있으며 코드에서 그 함수들을 끌어다 쓰기 위해 선언한것이다

* 헤더파일 선언은 코드의 맨 위, main함수 이전에 선언되어야 한다
<> 이 사이에 있는것이 헤더파일의 이름이다

c언어는 함수들의 조합으로 이루어져 있다.
C언어로 만들어진 모든 프로그램은 프로그램이 시작되면 main함수를 찾는다
즉 main 함수를 필수로 정의해야 한다는 것이다

int main(void) - 함수의 선언 방식이다
    가장 먼저 반환 타입     - 이 함수에서는 int형태의 반환값이 나올것이다
    함수 이름             - 메인 함수 프로그램이 시작되면 자동으로 호출되는 함수이다
    (입력 형태)           - 이 함수에서는 void형태의 입력값이 필요하다

{
    함수의 내용은 중괄호를 사용하여 묶는다
    또한 함수 내의 존재하는 문장은 ;을 붙여야 한다 모든 문장에 붙는 것은 아니지만 모든 문장에 붙여야 한다 생각하고 붙이는 습관을 들이도록 하자
    //후에 ;이 없어서 오류가 나는 경험을 해볼 수 있을 것이다.
   
    printf("Hello world! \n");

    return 0;
}
